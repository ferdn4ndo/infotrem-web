/**
 * Mirror of the SCSS breakpoint tokens from `src/styles/variables.scss`.
 * Keep these values aligned so JS media-query logic and SCSS breakpoints match.
 */
export const BREAKPOINT_MEDIUM_PX = 720
export const BREAKPOINT_LARGE_PX = 1080
